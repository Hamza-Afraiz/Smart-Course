apiVersion: v1
kind: Service
metadata:
  name: smartcourse-api
  namespace: smartcourse
  labels: { app: smartcourse-api }
spec:
  selector: { app: smartcourse-api }
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
