# Base path the SPA calls. Defaults to /api/v1, which the Vite dev server
# proxies to the FastAPI backend (see vite.config.ts).
VITE_API_BASE_URL=/api/v1

# Where the dev proxy forwards /api requests. Override if the backend is not
# on localhost:8000.
VITE_BACKEND_URL=http://localhost:8000
